Простой вариант, как запустить PHP сервер на своей машине.

```
docker compose up -d
```

теперь по адресу http://localhost:8080/ поднят ваш сервер.

Чтобы выключить, в терминале набираем

```
docker compose down
```